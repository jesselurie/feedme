import React, { Component } from 'react';
import './Header.css';

class Header extends Component {
  render() {
    return (
        <div className="bar large theme-d4">
          <span className="bar-item">FEED ME</span>
        </div>
    );
  }
}

export default Header;
